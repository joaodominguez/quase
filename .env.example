NEXT_PUBLIC_SITE_URL=https://quase.pt

# Backoffice (Basic Auth) — mesmo padrão do VagaSaúde
ADMIN_USER=admin
ADMIN_PASSWORD=change-me-with-a-long-random-password

# Onde o backoffice lê/grava sitios.json (em produção)
# DATA_DIR=/var/www/quase/data

# Fotos carregadas no admin (persistem entre deploys)
# SHARED_FOTOS=/var/www/quase/shared/fotos

